// <autogenerated>
//   This file was generated by T4 code generator AllViewModels.tt.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

using System.Collections.ObjectModel;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using System.Threading.Tasks.Schedulers;
using SimpleMvvmToolkit;
using System;
using System.Windows;
using System.Windows.Data;
using System.Text;
using Core.Common.UI.DataVirtualization;
using System.Collections.Generic;
using Core.Common.UI;
using Core.Common.Converters;

using OCR.Client.Entities;
using OCR.Client.Repositories;
//using WaterNut.Client.Repositories;
        
using CoreEntities.Client.Entities;


namespace WaterNut.QuerySpace.OCR.ViewModels
{
    
	public partial class StartViewModel_AutoGen : ViewModelBase<StartViewModel_AutoGen>
	{

       private static readonly StartViewModel_AutoGen instance;
       static StartViewModel_AutoGen()
        {
            instance = new StartViewModel_AutoGen();
        }

       public static StartViewModel_AutoGen Instance
        {
            get { return instance; }
        }

       private readonly object lockObject = new object();
  
  

        public StartViewModel_AutoGen()
        {
          
            RegisterToReceiveMessages<Start>(MessageToken.CurrentStartChanged, OnCurrentStartChanged);
            RegisterToReceiveMessages(MessageToken.StartChanged, OnStartChanged);
			RegisterToReceiveMessages(MessageToken.StartFilterExpressionChanged, OnStartFilterExpressionChanged);

 
			RegisterToReceiveMessages<Parts>(MessageToken.CurrentPartsChanged, OnCurrentPartsChanged);
 
			RegisterToReceiveMessages<RegExChain>(MessageToken.CurrentRegExChainChanged, OnCurrentRegExChainChanged);

 			// Recieve messages for Core Current Entities Changed
 

			Start = new VirtualList<Start>(vloader);
			Start.LoadingStateChanged += Start_LoadingStateChanged;
            BindingOperations.EnableCollectionSynchronization(Start, lockObject);
			
            OnCreated();        
            OnTotals();
        }

        partial void OnCreated();
        partial void OnTotals();

		private VirtualList<Start> _Start = null;
        public VirtualList<Start> Start
        {
            get
            {
                return _Start;
            }
            set
            {
                _Start = value;
            }
        }

		 private void OnStartFilterExpressionChanged(object sender, NotificationEventArgs e)
        {
			Start.Refresh();
            SelectedStart.Clear();
            NotifyPropertyChanged(x => SelectedStart);
            BeginSendMessage(MessageToken.SelectedStartChanged, new NotificationEventArgs(MessageToken.SelectedStartChanged));
        }

		void Start_LoadingStateChanged(object sender, EventArgs e)
        {
            switch (Start.LoadingState)
            {
                case QueuedBackgroundWorkerState.Processing:
                    StatusModel.Timer("Getting Data...");
                    break;
                case QueuedBackgroundWorkerState.Standby: 
                    StatusModel.StopStatusUpdate();
                    NotifyPropertyChanged(x => Start);
                    break;
                case QueuedBackgroundWorkerState.StoppedByError:
                    StatusModel.Error("Start | Error occured..." + Start.LastLoadingError.Message);
                    NotifyPropertyChanged(x => Start);
                    break;
            }
           
        }

		
		public readonly StartVirturalListLoader vloader = new StartVirturalListLoader();

		private ObservableCollection<Start> _selectedStart = new ObservableCollection<Start>();
        public ObservableCollection<Start> SelectedStart
        {
            get
            {
                return _selectedStart;
            }
            set
            {
                _selectedStart = value;
				BeginSendMessage(MessageToken.SelectedStartChanged,
                                    new NotificationEventArgs(MessageToken.SelectedStartChanged));
				 NotifyPropertyChanged(x => SelectedStart);
            }
        }

        internal virtual void OnCurrentStartChanged(object sender, NotificationEventArgs<Start> e)
        {
            if(BaseViewModel.Instance.CurrentStart != null) BaseViewModel.Instance.CurrentStart.PropertyChanged += CurrentStart__propertyChanged;
           // NotifyPropertyChanged(x => this.CurrentStart);
        }   

            void CurrentStart__propertyChanged(object sender, System.ComponentModel.PropertyChangedEventArgs e)
                {
                    //if (e.PropertyName == "AddParts")
                   // {
                   //    if(Parts.Contains(CurrentStart.Parts) == false) Parts.Add(CurrentStart.Parts);
                    //}
                    //if (e.PropertyName == "AddRegExChain")
                   // {
                   //    if(RegExChain.Contains(CurrentStart.RegExChain) == false) RegExChain.Add(CurrentStart.RegExChain);
                    //}
                 } 
        internal virtual void OnStartChanged(object sender, NotificationEventArgs e)
        {
            _Start.Refresh();
			NotifyPropertyChanged(x => this.Start);
        }   


 	
		 internal virtual void OnCurrentPartsChanged(object sender, SimpleMvvmToolkit.NotificationEventArgs<Parts> e)
			{
			if(ViewCurrentParts == false) return;
			if (e.Data == null || e.Data.Id == null)
                {
                    vloader.FilterExpression = "None";
                }
                else
                {
				vloader.FilterExpression = string.Format("PartId == {0}", e.Data.Id.ToString());
                 }

				Start.Refresh();
				NotifyPropertyChanged(x => this.Start);
                // SendMessage(MessageToken.StartChanged, new NotificationEventArgs(MessageToken.StartChanged));
                			}
	
		 internal virtual void OnCurrentRegExChainChanged(object sender, SimpleMvvmToolkit.NotificationEventArgs<RegExChain> e)
			{
			if(ViewCurrentRegExChain == false) return;
			if (e.Data == null || e.Data.Id == null)
                {
                    vloader.FilterExpression = "None";
                }
                else
                {
				vloader.FilterExpression = string.Format("RegExChainId == {0}", e.Data.Id.ToString());
                 }

				Start.Refresh();
				NotifyPropertyChanged(x => this.Start);
                // SendMessage(MessageToken.StartChanged, new NotificationEventArgs(MessageToken.StartChanged));
                			}

  			// Core Current Entities Changed
			// theorticall don't need this cuz i am inheriting from core entities baseview model so changes should flow up to here
  
// Filtering Each Field except IDs
 	
		 bool _viewCurrentParts = false;
         public bool ViewCurrentParts
         {
             get
             {
                 return _viewCurrentParts;
             }
             set
             {
                 _viewCurrentParts = value;
                 NotifyPropertyChanged(x => x.ViewCurrentParts);
                FilterData();
             }
         }
 	
		 bool _viewCurrentRegExChain = false;
         public bool ViewCurrentRegExChain
         {
             get
             {
                 return _viewCurrentRegExChain;
             }
             set
             {
                 _viewCurrentRegExChain = value;
                 NotifyPropertyChanged(x => x.ViewCurrentRegExChain);
                FilterData();
             }
         }
		public void ViewAll()
        {
		vloader.FilterExpression = "All";




			vloader.ClearNavigationExpression();
			_Start.Refresh();
			NotifyPropertyChanged(x => this.Start);
		}

		public async Task SelectAll()
        {
            IEnumerable<Start> lst = null;
            using (var ctx = new StartRepository())
            {
                lst = await ctx.GetStartByExpressionNav(vloader.FilterExpression, vloader.NavigationExpression).ConfigureAwait(continueOnCapturedContext: false);
            }
            SelectedStart = new ObservableCollection<Start>(lst);
        }

 
		internal bool DisableBaseFilterData = false;
        public virtual void FilterData()
	    {
	        FilterData(null);
	    }
		public void FilterData(StringBuilder res = null)
		{
		    if (DisableBaseFilterData) return;
			if(res == null) res = GetAutoPropertyFilterString();
			if (res.Length == 0 && vloader.NavigationExpression.Count != 0) res.Append("&& All");					
			if (res.Length > 0)
            {
                vloader.FilterExpression = res.ToString().Trim().Substring(2).Trim();
            }
            else
            {
                 if (vloader.FilterExpression != "All") vloader.FilterExpression = null;
            }

			Start.Refresh();
			NotifyPropertyChanged(x => this.Start);
		}		  



		internal virtual StringBuilder GetAutoPropertyFilterString()
		{
		var res = new StringBuilder();
			return res.ToString().StartsWith(" &&") || res.Length == 0 ? res:  res.Insert(0," && ");		
		}

// Send to Excel Implementation


        public async Task Send2Excel()
        {
			IEnumerable<Start> lst = null;
            using (var ctx = new StartRepository())
            {
                lst = await ctx.GetStartByExpressionNav(vloader.FilterExpression, vloader.NavigationExpression).ConfigureAwait(continueOnCapturedContext: false);
            }
             if (lst == null || !lst.Any()) 
              {
                   MessageBox.Show("No Data to Send to Excel");
                   return;
               }
            var s = new ExportToCSV<StartExcelLine, List<StartExcelLine>>
            {
                dataToPrint = lst.Select(x => new StartExcelLine
                {
                }).ToList()
            };
            using (var sta = new StaTaskScheduler(numberOfThreads: 1))
            {
               await Task.Factory.StartNew(s.GenerateReport, CancellationToken.None, TaskCreationOptions.None, sta).ConfigureAwait(false);
            }
        }

        public class StartExcelLine
        {
		        }

		
    }
}
		
