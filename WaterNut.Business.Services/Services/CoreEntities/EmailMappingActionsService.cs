// <autogenerated>
//   This file was generated by T4 code generator AllServices.tt.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>


using System;
using System.Collections.Generic;
using System.Collections.Concurrent;
using System.Data.Entity.Infrastructure;
using System.Data.Entity.Core;
using System.Data.SqlClient;
using System.Diagnostics;
using System.ServiceModel;
using System.Threading.Tasks;
//using System.Transactions;


using System.Linq.Dynamic;
using System.ComponentModel.Composition;
using CoreEntities.Business.Entities;
using Core.Common.Contracts;
using Core.Common.Business.Services;
using Core.Common.UI;

using System.Data.Entity;
using System.Linq;
using TrackableEntities;
using TrackableEntities.Common;
using TrackableEntities.EF6;
using WaterNut.Interfaces;

namespace CoreEntities.Business.Services
{
   [Export (typeof(IEmailMappingActionsService))]
   [Export(typeof(IBusinessService))]
   [PartCreationPolicy(CreationPolicy.NonShared)]
   [ServiceBehavior(InstanceContextMode = InstanceContextMode.PerCall,
                    ConcurrencyMode = ConcurrencyMode.Multiple)]
   
    public partial class EmailMappingActionsService : IEmailMappingActionsService, IDisposable
    {
        //private readonly CoreEntitiesContext dbContext;

        public bool StartTracking { get; set; }

        public EmailMappingActionsService()
        {
            try
            {
                // dbContext = new CoreEntitiesContext(){StartTracking = StartTracking};
                StartTracking = false;
             }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault, new FaultReason(fault.Message));
            }
        }

        public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActions(List<string> includesLst = null, bool tracking = true)
        {
            try
            {
            //using (var scope = new TransactionScope(TransactionScopeOption.Required,
                                   //new TransactionOptions() {IsolationLevel = IsolationLevel.ReadUncommitted}))
               // {
                  using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                  {
				    var set = AddIncludes(includesLst, dbContext);
                    IEnumerable<EmailMappingActions> entities = set.AsNoTracking().ToList();
                           //scope.Complete();
                            if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                            return Task.FromResult(entities);
                   }
                //}
             }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }


        public Task<EmailMappingActions> GetEmailMappingActionsByKey(string Id, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
			   if(string.IsNullOrEmpty(Id))return Task.FromResult<EmailMappingActions>(null); 
              using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(Id);
				var set = AddIncludes(includesLst, dbContext);
                EmailMappingActions entity = set.AsNoTracking().SingleOrDefault(x => x.Id == i);
                if(tracking && entity != null) entity.StartTracking();
                return Task.FromResult(entity);
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }


		 public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByExpression(string exp, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
					if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult<IEnumerable<EmailMappingActions>>(new List<EmailMappingActions>());
					var set = AddIncludes(includesLst, dbContext);
                    if (exp == "All")
                    {
						var entities = set.AsNoTracking().ToList();

                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EmailMappingActions>>(entities); 
                    }
					else
					{
						var entities = set.AsNoTracking().Where(exp)
											.ToList();
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EmailMappingActions>>(entities); 
											
					}
					
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		 public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByExpressionLst(List<string> expLst, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
					if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return Task.FromResult<IEnumerable<EmailMappingActions>>(new List<EmailMappingActions>());
					var set = AddIncludes(includesLst, dbContext);
                    if (expLst.FirstOrDefault() == "All")
                    {
						var entities = set.AsNoTracking().ToList(); 
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EmailMappingActions>>(entities); 
                    }
					else
					{
						set = AddWheres(expLst, set);
						var entities = set.AsNoTracking().ToList();
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EmailMappingActions>>(entities); 
											
					}
					
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByExpressionNav(string exp,
																							  Dictionary<string, string> navExp,
																							  List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (string.IsNullOrEmpty(exp) || exp == "None") return new List<EmailMappingActions>();

                    if (exp == "All" && navExp.Count == 0)
                    {
                        var aentities = AddIncludes(includesLst, dbContext)
												.ToList();
                        if(tracking) aentities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return aentities; 
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "Actions":
                                return
                                    await
                                        GetWhere<Actions>(dbContext, exp, itm.Value, "EmailMappingActions", "SelectMany", includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);

                            case "EmailMapping":
                                return
                                    await
                                        GetWhere<EmailMapping>(dbContext, exp, itm.Value, "EmailMappingActions", "SelectMany", includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);

                        }

                    }
					var set = AddIncludes(includesLst, dbContext);
                    var entities = set.AsNoTracking().Where(exp)
									.ToList();
                    if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 

                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByBatch(string exp,
                                                                                    int totalrow, List<string> includesLst = null, bool tracking = true)
        {
            try
            {

                var res = new ConcurrentQueue<List<EmailMappingActions>>();



                if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult<IEnumerable<EmailMappingActions>>(new List<EmailMappingActions>());


                var batchSize = 500;
                var batches = Convert.ToInt32(totalrow / batchSize);

                if (totalrow % batchSize > 0) batches += 1;
                var exceptions = new ConcurrentQueue<Exception>();
                Parallel.For(0, batches,
                   new ParallelOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount * 2 },
                    bat =>
                    //  for (int bat = 0; bat < batches; bat++)
                    {
                        try
                        {
                            using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                            {
                                dbContext.Database.CommandTimeout = 0;
                                dbContext.Configuration.AutoDetectChangesEnabled = false;
                                //dbContext.Configuration.LazyLoadingEnabled = true;
                                var set = AddIncludes(includesLst, dbContext);
                                IQueryable<EmailMappingActions> dset;
                                if (exp == "All")
                                {
                                    dset = set.OrderBy(x => x.Id);
                                }
                                else
                                {
                                    dset = set.OrderBy(x => x.Id).Where(exp);
                                }

                                var lst = dset.AsNoTracking()
                                    .Skip(bat * batchSize)
                                    .Take(batchSize)
                                    .ToList();
                                res.Enqueue(lst);
                            }

                        }
                        catch (Exception ex)
                        {
                            exceptions.Enqueue(ex);
                        }
                    }
                    );
                if (exceptions.Count > 0) throw new AggregateException(exceptions);
    
                var entities = res.SelectMany(x => x.ToList());
                if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                return Task.FromResult(entities); 

            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }
        public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByBatchExpressionLst(List<string> expLst,
                                                                                                 int totalrow, List<string> includesLst = null, bool tracking = true)
        {
            try
            {

                var res = new ConcurrentQueue<List<EmailMappingActions>>();



                if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return Task.FromResult<IEnumerable<EmailMappingActions>>(new List<EmailMappingActions>());


                var batchSize = 500;
                var batches = Convert.ToInt32(totalrow / batchSize);

                if (totalrow % batchSize > 0) batches += 1;
                var exceptions = new ConcurrentQueue<Exception>();
                Parallel.For(0, batches,
                   new ParallelOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount * 2 },
                    bat =>
                    //  for (int bat = 0; bat < batches; bat++)
                    {
                        try
                        {
                            using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                            {
                                dbContext.Database.CommandTimeout = 0;
                                dbContext.Configuration.AutoDetectChangesEnabled = false;
                                //dbContext.Configuration.LazyLoadingEnabled = true;
                                var set = AddIncludes(includesLst, dbContext);
                                IQueryable<EmailMappingActions> dset;
                                if (expLst.FirstOrDefault() == "All")
                                {
                                    dset = set.OrderBy(x => x.Id);
                                }
                                else
                                {
                                    set = AddWheres(expLst, set);
                                    dset = set.OrderBy(x => x.Id);
                                }

                                var lst = dset.AsNoTracking()
                                    .Skip(bat * batchSize)
                                    .Take(batchSize)
                                    .ToList();
                                res.Enqueue(lst);
                            }

                        }
                        catch (Exception ex)
                        {
                            exceptions.Enqueue(ex);
                        }
                    }
                    );
                if (exceptions.Count > 0) throw new AggregateException(exceptions);
                var entities = res.SelectMany(x => x.ToList());
                if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                return Task.FromResult(entities); 
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }


        public Task<EmailMappingActions> UpdateEmailMappingActions(EmailMappingActions entity)
        { 
            using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
              {
                try
                {   
                     var res = (EmailMappingActions) entity;
                    if(res.TrackingState == TrackingState.Unchanged) res.TrackingState = TrackingState.Modified;                              
                    
                    dbContext.ApplyChanges(res);
                    dbContext.SaveChanges();
                    res.AcceptChanges();
                    return Task.FromResult(res);      
      
                }
                catch (DbUpdateConcurrencyException dce)
                {
                    // Get failed entry
                    foreach (var itm in dce.Entries)
                    {
                        if(itm.State != EntityState.Added)
                         {
                            var dv = itm.GetDatabaseValues();
                            if(dv != null) itm.OriginalValues.SetValues(dv);
                        }
                    }
                }
                catch (OptimisticConcurrencyException oce)
                {
                    var context = ((IObjectContextAdapter)dbContext).ObjectContext;

                    foreach (var entry in oce.StateEntries)
                    {
                        context.Refresh(System.Data.Entity.Core.Objects.RefreshMode.StoreWins, entry.Entity);
                    }
                }   
                catch (DbUpdateException e)
                {
                    
                   // Debugger.Break();
                    SqlException s = e.InnerException.InnerException as SqlException;
                    if (s != null && s.Number == 2627)
                    {
                         
                    }
                    else
                    {
                        Debugger.Break();
                        throw;
                    }
                }             
                catch (Exception updateEx)
                {
                    if (
                        updateEx.Message.Contains(
                            "The changes to the database were committed successfully, " +
                            "but an error occurred while updating the object context"))
                        return Task.FromResult(entity);

                    System.Diagnostics.Debugger.Break();
                    //throw new FaultException(updateEx.Message);
                        var fault = new ValidationFault
                                    {
                                        Result = false,
                                        Message = updateEx.Message,
                                        Description = updateEx.StackTrace
                                    };
                        throw new FaultException<ValidationFault>(fault);
                }
            }
           return Task.FromResult(entity);
        }

        public Task<EmailMappingActions> CreateEmailMappingActions(EmailMappingActions entity)
        {
            try
            {
                var res = (EmailMappingActions) entity;
              using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
              {
                dbContext.EmailMappingActions.Add(res);
                dbContext.SaveChanges();
                res.AcceptChanges();
                return Task.FromResult(res);
              }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public Task<bool> DeleteEmailMappingActions(string Id)
        {
            try
            {
              using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(Id);
                EmailMappingActions entity = dbContext.EmailMappingActions
													.SingleOrDefault(x => x.Id == i);
                if (entity == null)
                    return Task.FromResult(false);

                    dbContext.EmailMappingActions.Attach(entity);
                    dbContext.EmailMappingActions.Remove(entity);
                    dbContext.SaveChanges();
                    return Task.FromResult(true);
              }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public async Task<bool> RemoveSelectedEmailMappingActions(IEnumerable<string> lst)
        {
            try
            {
                StatusModel.StartStatusUpdate("Removing EmailMappingActions", lst.Count());
                var t = Task.Run(() =>
                {
                    using (var ctx = new EmailMappingActionsService())
                    {
                        foreach (var item in lst.ToList())
                        {

                            ctx.DeleteEmailMappingActions(item).Wait();
                            StatusModel.StatusUpdate();
                        }
                    }
                });
                await t.ConfigureAwait(false);
                return true;
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
            

               
        }

		// Virtural list Implementation

         public Task<int> CountByExpressionLst(List<string> expLst)
        {
            try
            {
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return Task.FromResult(0);
                    var set = (IQueryable<EmailMappingActions>)dbContext.EmailMappingActions; 
                    if (expLst.FirstOrDefault() == "All")
                    {
                        return Task.FromResult(set.AsNoTracking().Count());
                    }
                    else
                    {
                        set = AddWheres(expLst, set);
                        return Task.FromResult(set.AsNoTracking().Count());
                    }
                    
                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		public Task<int> Count(string exp)
        {
            try
            {
                using (CoreEntitiesContext dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult(0);
                    if (exp == "All")
                    {
                        return Task.FromResult(dbContext.EmailMappingActions
                            .AsNoTracking()
                            .Count());
                    }
                    else
                    {
                        
                        return Task.FromResult(dbContext.EmailMappingActions
                            .AsNoTracking()
                            .Where(exp)
                            .Count());
                    }
                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
        
        public Task<IEnumerable<EmailMappingActions>> LoadRange(int startIndex, int count, string exp)
        {
            try
            {
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult<IEnumerable<EmailMappingActions>>(new List<EmailMappingActions>());
                    if (exp == "All")
                    {
                        return Task.FromResult<IEnumerable<EmailMappingActions>>(dbContext.EmailMappingActions
                            .AsNoTracking()
                            .OrderBy(y => y.Id)
                            .Skip(startIndex)
                            .Take(count)
                            .ToList());
                    }
                    else
                    {
                        
                        return Task.FromResult<IEnumerable<EmailMappingActions>>(dbContext.EmailMappingActions
                            .AsNoTracking()
                            .Where(exp)
                            .OrderBy(y => y.Id)
                            .Skip(startIndex)
                            .Take(count)
                            .ToList());
                    }
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<int> CountNav(string exp, Dictionary<string, string> navExp)
        {
            try
            {
                if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (exp == "All" && navExp.Count == 0)
                    {
                        return dbContext.EmailMappingActions
										.AsNoTracking()
                                        .Count();
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "Actions":
                                return await CountWhere<Actions>(dbContext, exp, itm.Value, "EmailMappingActions", "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
                            case "EmailMapping":
                                return await CountWhere<EmailMapping>(dbContext, exp, itm.Value, "EmailMappingActions", "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
						}
                    }
                    return dbContext.EmailMappingActions.Where(exp == "All" || exp == null ? "Id != null" : exp)
											.AsNoTracking()
                                            .Count();
                }
                
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<int> CountWhere<T>(CoreEntitiesContext dbContext, string exp, string navExp, string navProp, string rel) where T : class
        {
              switch (rel)
		    {
		        case "SelectMany":
                    return await CountWhereSelectMany<T>(dbContext, exp, navExp, navProp)
									.ConfigureAwait(continueOnCapturedContext: false);
		            
                default:
                    return await CountWhereSelect<T>(dbContext, exp, navExp, navProp)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static Task<int> CountWhereSelectMany<T>(CoreEntitiesContext dbContext, string exp, string navExp, string navProp) where T : class
        {
			try
			{
            return Task.FromResult(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EmailMappingActions>()
                .Where(exp == "All" || exp == null ? "Id != null" : exp)
                .Distinct()
                .OrderBy("Id")
                .Count());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<int> CountWhereSelect<T>(CoreEntitiesContext dbContext, string exp, string navExp, string navProp) where T : class
        {
			try
			{
            return Task.FromResult(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EmailMappingActions>()
                .Where(exp == "All" || exp == null ? "Id != null" : exp)
                .Distinct()
                .OrderBy("Id")
                .Count());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		  public async Task<IEnumerable<EmailMappingActions>> LoadRangeNav(int startIndex, int count, string exp,
                                                                                 Dictionary<string, string> navExp, IEnumerable<string> includeLst = null)
        {
            try
            {
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if ((string.IsNullOrEmpty(exp) && navExp.Count == 0) || exp == "None") return new List<EmailMappingActions>();
                    var set = AddIncludes(includeLst, dbContext);

                    if (exp == "All" && navExp.Count == 0)
                    {
                       
                        return set
									.AsNoTracking()
                                    .OrderBy(y => y.Id)
 
                                    .Skip(startIndex)
                                    .Take(count)
									.ToList();
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "Actions":
                                return
                                    await
                                        LoadRangeWhere<Actions>(startIndex, count, dbContext, exp, itm.Value, "EmailMappingActions", "SelectMany")
													.ConfigureAwait(continueOnCapturedContext: false);

                            case "EmailMapping":
                                return
                                    await
                                        LoadRangeWhere<EmailMapping>(startIndex, count, dbContext, exp, itm.Value, "EmailMappingActions", "SelectMany")
													.ConfigureAwait(continueOnCapturedContext: false);

                          
							default:
                                throw new ArgumentException("No Navigation property found for " + itm.Key);
						}

                    }
                    return set//dbContext.EmailMappingActions
								.AsNoTracking()
                                .Where(exp == "All" || exp == null ? "Id != null" : exp)
								.OrderBy(y => y.Id)
 
                                .Skip(startIndex)
                                .Take(count)
								.ToList();


                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<IEnumerable<EmailMappingActions>> LoadRangeWhere<T>(int startIndex, int count,
            CoreEntitiesContext dbContext, string exp, string navExp, string navProp, string rel, IEnumerable<string> includeLst = null) where T : class
        {
             switch (rel)
		    {
		        case "SelectMany":
                    return await LoadRangeSelectMany<T>(startIndex, count, dbContext, exp, navExp, navProp, includeLst)
									.ConfigureAwait(continueOnCapturedContext: false);
		            
                default:
                    return await LoadRangeSelect<T>(startIndex, count, dbContext, exp, navExp, navProp, includeLst)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static Task<IEnumerable<EmailMappingActions>> LoadRangeSelectMany<T>(int startIndex, int count,
                                                                                     CoreEntitiesContext dbContext, string exp, string navExp, string navProp, IEnumerable<string> includeLst = null) where T : class
        {
			try
			{
            var set = dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EmailMappingActions>();
    
            if (includeLst != null) set = includeLst.Aggregate(set, (current, itm) => current.Include(itm));            

            return Task.FromResult<IEnumerable<EmailMappingActions>>(set
                .Where(exp == "All" || exp == null ? "Id != null" : exp)
                .Distinct()
                .OrderBy(y => y.Id)
 
                .Skip(startIndex)
                .Take(count)
                .ToList());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<IEnumerable<EmailMappingActions>> LoadRangeSelect<T>(int startIndex, int count,
                                                                                 CoreEntitiesContext dbContext, string exp, string navExp, string navProp, IEnumerable<string> includeLst = null) where T : class
        {
			try
			{
              var set = dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EmailMappingActions>();

               if (includeLst != null) set = includeLst.Aggregate(set, (current, itm) => current.Include(itm)); 
                
               return Task.FromResult<IEnumerable<EmailMappingActions>>(set
                   .Where(exp == "All" || exp == null ? "Id != null" : exp)
                   .Distinct()
                   .OrderBy(y => y.Id)
 
                   .Skip(startIndex)
                   .Take(count)
                   .ToList());
							 }
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

        private static async Task<IEnumerable<EmailMappingActions>> GetWhere<T>(CoreEntitiesContext dbContext,
            string exp, string navExp, string navProp, string rel, List<string> includesLst = null) where T : class
        {
			try
			{
			    switch (rel)
				{
					case "SelectMany":
						return await GetWhereSelectMany<T>(dbContext, exp, navExp, navProp, includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);
						
					default:
						return await GetWhereSelect<T>(dbContext, exp, navExp, navProp, includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);
				}
			
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<IEnumerable<EmailMappingActions>> GetWhereSelectMany<T>(CoreEntitiesContext dbContext,
                                                                                    string exp, string navExp, string navProp, List<string> includesLst = null) where T : class
        {
			try
			{

			if (includesLst == null)
			{
				return Task.FromResult<IEnumerable<EmailMappingActions>>(dbContext.Set<T>()
                    .AsNoTracking()
                    .Where(navExp)
                    .SelectMany(navProp).OfType<EmailMappingActions>()
                    .Where(exp == "All" || exp == null?"Id != null":exp)
                    .Distinct()
                    .ToList());
			}

			var set = (DbQuery<EmailMappingActions>)dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EmailMappingActions>()
                .Where(exp == "All" || exp == null?"Id != null":exp)
                .Distinct();

			set = includesLst.Aggregate(set, (current, itm) => current.Include(itm));

            return Task.FromResult<IEnumerable<EmailMappingActions>>(set.ToList());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<IEnumerable<EmailMappingActions>> GetWhereSelect<T>(CoreEntitiesContext dbContext,
                                                                                string exp, string navExp, string navProp, List<string> includesLst = null) where T : class
        {
			try
			{

			if (includesLst == null)
			{
				return Task.FromResult<IEnumerable<EmailMappingActions>>(dbContext.Set<T>()
                    .AsNoTracking()
                    .Where(navExp)
                    .Select(navProp).OfType<EmailMappingActions>()
                    .Where(exp == "All" || exp == null?"Id != null":exp)
                    .Distinct()
                    .ToList());
			}

			var set = (DbQuery<EmailMappingActions>)dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EmailMappingActions>()
                .Where(exp == "All" || exp == null?"Id != null":exp)
                .Distinct();

			set = includesLst.Aggregate(set, (current, itm) => current.Include(itm));

            return Task.FromResult<IEnumerable<EmailMappingActions>>(set.ToList());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

			        public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByEmailMappingId(string EmailMappingId, List<string> includesLst = null)
        {
            try
            {
                using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(EmailMappingId);
                var set = AddIncludes(includesLst, dbContext);
                IEnumerable<EmailMappingActions> entities = set//dbContext.EmailMappingActions
                                      .AsNoTracking()
                                        .Where(x => x.EmailMappingId.ToString() == EmailMappingId.ToString())
										.ToList();
                return Task.FromResult(entities);
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
 	        public Task<IEnumerable<EmailMappingActions>> GetEmailMappingActionsByActionId(string ActionId, List<string> includesLst = null)
        {
            try
            {
                using ( var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(ActionId);
                var set = AddIncludes(includesLst, dbContext);
                IEnumerable<EmailMappingActions> entities = set//dbContext.EmailMappingActions
                                      .AsNoTracking()
                                        .Where(x => x.ActionId.ToString() == ActionId.ToString())
										.ToList();
                return Task.FromResult(entities);
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
 
		public decimal SumField(string whereExp, string field)
         {
             try
             {
                 using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                 {
                    dbContext.Database.CommandTimeout = 0;
					decimal res = 0;
                     if (string.IsNullOrEmpty(whereExp) || whereExp == "None") return 0;
                     if (whereExp == "All")
                     {
                          res = Convert.ToDecimal(dbContext.EmailMappingActions.AsNoTracking().Sum(field));
                     }
                     else
                     {
                         res = Convert.ToDecimal(dbContext.EmailMappingActions.AsNoTracking().Where(whereExp).Sum(field));
                     }
                     
                     return res;
                 }
             }
             catch (Exception updateEx)
             {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
             }
         }

        public async Task<decimal> SumNav( string exp, Dictionary<string, string> navExp, string field)
        {
            try
            {
                if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (!dbContext.EmailMappingActions.Any()) return 0;
                    if (exp == "All" && navExp.Count == 0)
                    {
                        return Convert.ToDecimal(dbContext.EmailMappingActions
										.AsNoTracking()
                                        .Sum(field)??0);
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "Actions":
                                return await SumWhere<Actions>(dbContext, exp, itm.Value, "EmailMappingActions", field, "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
                            case "EmailMapping":
                                return await SumWhere<EmailMapping>(dbContext, exp, itm.Value, "EmailMappingActions", field, "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
						}
                    }
                    return Convert.ToDecimal(dbContext.EmailMappingActions.Where(exp == "All" || exp == null ? "Id != null" : exp)
											.AsNoTracking()
                                            .Sum(field)??0);
                }
                
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<decimal> SumWhere<T>(CoreEntitiesContext dbContext, string exp, string navExp, string navProp, string field, string rel) where T : class
        {
              switch (rel)
		    {
		        case "SelectMany":
                    return await SumWhereSelectMany<T>(dbContext, exp, navExp, navProp, field)
									.ConfigureAwait(continueOnCapturedContext: false);
		        default:
                    return await SumWhereSelect<T>(dbContext, exp, navExp, navProp, field)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static Task<decimal> SumWhereSelectMany<T>(CoreEntitiesContext dbContext, string exp, string navExp, string navProp, string field) where T : class
        {
			try
			{
            return Task.FromResult(Convert.ToDecimal(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EmailMappingActions>()
                .Where(exp == "All" || exp == null ? "Id != null" : exp)
                .Distinct()
                .OrderBy("Id")
                .Sum(field)));
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<decimal> SumWhereSelect<T>(CoreEntitiesContext dbContext, string exp, string navExp, string navProp, string field) where T : class
        {
			try
			{
            return Task.FromResult(Convert.ToDecimal(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EmailMappingActions>()
                .Where(exp == "All" || exp == null ? "Id != null" : exp)
                .Distinct()
                .OrderBy("Id")
                .Sum(field)));
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }



		 public string MinField(string whereExp, string field)
         {
             try
             {
                 using (var dbContext = new CoreEntitiesContext(){StartTracking = StartTracking})
                 {
                    dbContext.Database.CommandTimeout = 0;
					string res = "";
                     if (string.IsNullOrEmpty(whereExp) || whereExp == "None") return res;
                     if (whereExp == "All")
                     {
                          res = Convert.ToString(dbContext.EmailMappingActions.AsNoTracking().Min(field));
                     }
                     else
                     {
                         res = Convert.ToString(dbContext.EmailMappingActions.AsNoTracking().Where(whereExp).Min(field));
                     }
                     
                     return res;
                 }
             }
             catch (Exception updateEx)
             {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
             }
         }

		 
		private static IQueryable<EmailMappingActions> AddIncludes(IEnumerable<string> includesLst, CoreEntitiesContext dbContext)
       {
		 try
			{
			   if (includesLst == null) includesLst = new List<string>();
			   var set =(DbQuery<EmailMappingActions>) dbContext.EmailMappingActions; 
			   set = includesLst.Where(x => !string.IsNullOrEmpty(x))
                                .Aggregate(set, (current, itm) => current.Include(itm));
			   return set;
			 }
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
       }
	   private IQueryable<EmailMappingActions> AddWheres(List<string> expLst, IQueryable<EmailMappingActions> set)
        {
            try
            {
                return expLst.Where(x => !string.IsNullOrEmpty(x))
                             .Aggregate(set, (current, itm) => current.Where(itm));
            }
            catch (Exception)
            {
                Debugger.Break();
                throw;
            }
          
        }

        public void Dispose()
        {
            try
            {
               // var dispose = dbContext as IDisposable;
             ////   if (dispose != null)
            //    {
           //         dbContext.Dispose();
           //     }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }

        }

    }
}



