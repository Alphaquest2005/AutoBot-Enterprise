// <autogenerated>
//   This file was generated by T4 code generator AllServices.tt.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>


using System;
using System.Collections.Generic;
using System.Collections.Concurrent;
using System.Data.Entity.Infrastructure;
using System.Data.Entity.Core;
using System.Data.SqlClient;
using System.Diagnostics;
using System.ServiceModel;
using System.Threading.Tasks;
//using System.Transactions;


using System.Linq.Dynamic;
using System.ComponentModel.Composition;
using PreviousDocumentDS.Business.Entities;
using Core.Common.Contracts;
using Core.Common.Business.Services;
using Core.Common.UI;

using System.Data.Entity;
using System.Linq;
using TrackableEntities;
using TrackableEntities.Common;
using TrackableEntities.EF6;
using WaterNut.Interfaces;

namespace PreviousDocumentDS.Business.Services
{
   [Export (typeof(IEntryPreviousItemsService))]
   [Export(typeof(IBusinessService))]
   [PartCreationPolicy(CreationPolicy.NonShared)]
   [ServiceBehavior(InstanceContextMode = InstanceContextMode.PerCall,
                    ConcurrencyMode = ConcurrencyMode.Multiple)]
   
    public partial class EntryPreviousItemsService : IEntryPreviousItemsService, IDisposable
    {
        //private readonly PreviousDocumentDSContext dbContext;

        public bool StartTracking { get; set; }

        public EntryPreviousItemsService()
        {
            try
            {
                // dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking};
                StartTracking = false;
             }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault, new FaultReason(fault.Message));
            }
        }

        public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItems(List<string> includesLst = null, bool tracking = true)
        {
            try
            {
            //using (var scope = new TransactionScope(TransactionScopeOption.Required,
                                   //new TransactionOptions() {IsolationLevel = IsolationLevel.ReadUncommitted}))
               // {
                  using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                  {
				    var set = AddIncludes(includesLst, dbContext);
                    IEnumerable<EntryPreviousItems> entities = set.AsNoTracking().ToList();
                           //scope.Complete();
                            if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                            return Task.FromResult(entities);
                   }
                //}
             }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }


        public Task<EntryPreviousItems> GetEntryPreviousItemsByKey(string EntryPreviousItemId, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
			   if(string.IsNullOrEmpty(EntryPreviousItemId))return Task.FromResult<EntryPreviousItems>(null); 
              using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(EntryPreviousItemId);
				var set = AddIncludes(includesLst, dbContext);
                EntryPreviousItems entity = set.AsNoTracking().SingleOrDefault(x => x.EntryPreviousItemId == i);
                if(tracking && entity != null) entity.StartTracking();
                return Task.FromResult(entity);
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }


		 public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByExpression(string exp, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
					if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult<IEnumerable<EntryPreviousItems>>(new List<EntryPreviousItems>());
					var set = AddIncludes(includesLst, dbContext);
                    if (exp == "All")
                    {
						var entities = set.AsNoTracking().ToList();

                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EntryPreviousItems>>(entities); 
                    }
					else
					{
						var entities = set.AsNoTracking().Where(exp)
											.ToList();
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EntryPreviousItems>>(entities); 
											
					}
					
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		 public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByExpressionLst(List<string> expLst, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
					if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return Task.FromResult<IEnumerable<EntryPreviousItems>>(new List<EntryPreviousItems>());
					var set = AddIncludes(includesLst, dbContext);
                    if (expLst.FirstOrDefault() == "All")
                    {
						var entities = set.AsNoTracking().ToList(); 
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EntryPreviousItems>>(entities); 
                    }
					else
					{
						set = AddWheres(expLst, set);
						var entities = set.AsNoTracking().ToList();
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return Task.FromResult<IEnumerable<EntryPreviousItems>>(entities); 
											
					}
					
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByExpressionNav(string exp,
																							  Dictionary<string, string> navExp,
																							  List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (string.IsNullOrEmpty(exp) || exp == "None") return new List<EntryPreviousItems>();

                    if (exp == "All" && navExp.Count == 0)
                    {
                        var aentities = AddIncludes(includesLst, dbContext)
												.ToList();
                        if(tracking) aentities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return aentities; 
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "xcuda_Item":
                                return
                                    await
                                        GetWhere<PreviousEntry>(dbContext, exp, itm.Value, "EntryPreviousItems", "SelectMany", includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);

                        }

                    }
					var set = AddIncludes(includesLst, dbContext);
                    var entities = set.AsNoTracking().Where(exp)
									.ToList();
                    if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 

                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByBatch(string exp,
                                                                                  int totalrow, List<string> includesLst = null, bool tracking = true)
        {
            try
            {

                var res = new ConcurrentQueue<List<EntryPreviousItems>>();



                if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult<IEnumerable<EntryPreviousItems>>(new List<EntryPreviousItems>());


                var batchSize = 500;
                var batches = Convert.ToInt32(totalrow / batchSize);

                if (totalrow % batchSize > 0) batches += 1;
                var exceptions = new ConcurrentQueue<Exception>();
                Parallel.For(0, batches,
                   new ParallelOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount * 2 },
                    bat =>
                    //  for (int bat = 0; bat < batches; bat++)
                    {
                        try
                        {
                            using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                            {
                                dbContext.Database.CommandTimeout = 0;
                                dbContext.Configuration.AutoDetectChangesEnabled = false;
                                //dbContext.Configuration.LazyLoadingEnabled = true;
                                var set = AddIncludes(includesLst, dbContext);
                                IQueryable<EntryPreviousItems> dset;
                                if (exp == "All")
                                {
                                    dset = set.OrderBy(x => x.EntryPreviousItemId);
                                }
                                else
                                {
                                    dset = set.OrderBy(x => x.EntryPreviousItemId).Where(exp);
                                }

                                var lst = dset.AsNoTracking()
                                    .Skip(bat * batchSize)
                                    .Take(batchSize)
                                    .ToList();
                                res.Enqueue(lst);
                            }

                        }
                        catch (Exception ex)
                        {
                            exceptions.Enqueue(ex);
                        }
                    }
                    );
                if (exceptions.Count > 0) throw new AggregateException(exceptions);
    
                var entities = res.SelectMany(x => x.ToList());
                if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                return Task.FromResult(entities); 

            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }
        public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByBatchExpressionLst(List<string> expLst,
                                                                                               int totalrow, List<string> includesLst = null, bool tracking = true)
        {
            try
            {

                var res = new ConcurrentQueue<List<EntryPreviousItems>>();



                if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return Task.FromResult<IEnumerable<EntryPreviousItems>>(new List<EntryPreviousItems>());


                var batchSize = 500;
                var batches = Convert.ToInt32(totalrow / batchSize);

                if (totalrow % batchSize > 0) batches += 1;
                var exceptions = new ConcurrentQueue<Exception>();
                Parallel.For(0, batches,
                   new ParallelOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount * 2 },
                    bat =>
                    //  for (int bat = 0; bat < batches; bat++)
                    {
                        try
                        {
                            using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                            {
                                dbContext.Database.CommandTimeout = 0;
                                dbContext.Configuration.AutoDetectChangesEnabled = false;
                                //dbContext.Configuration.LazyLoadingEnabled = true;
                                var set = AddIncludes(includesLst, dbContext);
                                IQueryable<EntryPreviousItems> dset;
                                if (expLst.FirstOrDefault() == "All")
                                {
                                    dset = set.OrderBy(x => x.EntryPreviousItemId);
                                }
                                else
                                {
                                    set = AddWheres(expLst, set);
                                    dset = set.OrderBy(x => x.EntryPreviousItemId);
                                }

                                var lst = dset.AsNoTracking()
                                    .Skip(bat * batchSize)
                                    .Take(batchSize)
                                    .ToList();
                                res.Enqueue(lst);
                            }

                        }
                        catch (Exception ex)
                        {
                            exceptions.Enqueue(ex);
                        }
                    }
                    );
                if (exceptions.Count > 0) throw new AggregateException(exceptions);
                var entities = res.SelectMany(x => x.ToList());
                if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                return Task.FromResult(entities); 
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }


        public Task<EntryPreviousItems> UpdateEntryPreviousItems(EntryPreviousItems entity)
        { 
            using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
              {
                try
                {   
                     var res = (EntryPreviousItems) entity;
                    if(res.TrackingState == TrackingState.Unchanged) res.TrackingState = TrackingState.Modified;                              
                    
                    dbContext.ApplyChanges(res);
                    dbContext.SaveChanges();
                    res.AcceptChanges();
                    return Task.FromResult(res);      
      
                }
                catch (DbUpdateConcurrencyException dce)
                {
                    // Get failed entry
                    foreach (var itm in dce.Entries)
                    {
                        if(itm.State != EntityState.Added)
                         {
                            var dv = itm.GetDatabaseValues();
                            if(dv != null) itm.OriginalValues.SetValues(dv);
                        }
                    }
                }
                catch (OptimisticConcurrencyException oce)
                {
                    var context = ((IObjectContextAdapter)dbContext).ObjectContext;

                    foreach (var entry in oce.StateEntries)
                    {
                        context.Refresh(System.Data.Entity.Core.Objects.RefreshMode.StoreWins, entry.Entity);
                    }
                }   
                catch (DbUpdateException e)
                {
                    
                   // Debugger.Break();
                    SqlException s = e.InnerException.InnerException as SqlException;
                    if (s != null && s.Number == 2627)
                    {
                         
                    }
                    else
                    {
                        Debugger.Break();
                        throw;
                    }
                }             
                catch (Exception updateEx)
                {
                    if (
                        updateEx.Message.Contains(
                            "The changes to the database were committed successfully, " +
                            "but an error occurred while updating the object context"))
                        return Task.FromResult(entity);

                    System.Diagnostics.Debugger.Break();
                    //throw new FaultException(updateEx.Message);
                        var fault = new ValidationFault
                                    {
                                        Result = false,
                                        Message = updateEx.Message,
                                        Description = updateEx.StackTrace
                                    };
                        throw new FaultException<ValidationFault>(fault);
                }
            }
           return Task.FromResult(entity);
        }

        public Task<EntryPreviousItems> CreateEntryPreviousItems(EntryPreviousItems entity)
        {
            try
            {
                var res = (EntryPreviousItems) entity;
              using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
              {
                dbContext.EntryPreviousItems.Add(res);
                dbContext.SaveChanges();
                res.AcceptChanges();
                return Task.FromResult(res);
              }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public Task<bool> DeleteEntryPreviousItems(string EntryPreviousItemId)
        {
            try
            {
              using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(EntryPreviousItemId);
                EntryPreviousItems entity = dbContext.EntryPreviousItems
													.SingleOrDefault(x => x.EntryPreviousItemId == i);
                if (entity == null)
                    return Task.FromResult(false);

                    dbContext.EntryPreviousItems.Attach(entity);
                    dbContext.EntryPreviousItems.Remove(entity);
                    dbContext.SaveChanges();
                    return Task.FromResult(true);
              }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public async Task<bool> RemoveSelectedEntryPreviousItems(IEnumerable<string> lst)
        {
            try
            {
                StatusModel.StartStatusUpdate("Removing EntryPreviousItems", lst.Count());
                var t = Task.Run(() =>
                {
                    using (var ctx = new EntryPreviousItemsService())
                    {
                        foreach (var item in lst.ToList())
                        {

                            ctx.DeleteEntryPreviousItems(item).Wait();
                            StatusModel.StatusUpdate();
                        }
                    }
                });
                await t.ConfigureAwait(false);
                return true;
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
            

               
        }

		// Virtural list Implementation

         public Task<int> CountByExpressionLst(List<string> expLst)
        {
            try
            {
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return Task.FromResult(0);
                    var set = (IQueryable<EntryPreviousItems>)dbContext.EntryPreviousItems; 
                    if (expLst.FirstOrDefault() == "All")
                    {
                        return Task.FromResult(set.AsNoTracking().Count());
                    }
                    else
                    {
                        set = AddWheres(expLst, set);
                        return Task.FromResult(set.AsNoTracking().Count());
                    }
                    
                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		public Task<int> Count(string exp)
        {
            try
            {
                using (PreviousDocumentDSContext dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult(0);
                    if (exp == "All")
                    {
                        return Task.FromResult(dbContext.EntryPreviousItems
                            .AsNoTracking()
                            .Count());
                    }
                    else
                    {
                        
                        return Task.FromResult(dbContext.EntryPreviousItems
                            .AsNoTracking()
                            .Where(exp)
                            .Count());
                    }
                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
        
        public Task<IEnumerable<EntryPreviousItems>> LoadRange(int startIndex, int count, string exp)
        {
            try
            {
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (string.IsNullOrEmpty(exp) || exp == "None") return Task.FromResult<IEnumerable<EntryPreviousItems>>(new List<EntryPreviousItems>());
                    if (exp == "All")
                    {
                        return Task.FromResult<IEnumerable<EntryPreviousItems>>(dbContext.EntryPreviousItems
                            .AsNoTracking()
                            .OrderBy(y => y.EntryPreviousItemId)
                            .Skip(startIndex)
                            .Take(count)
                            .ToList());
                    }
                    else
                    {
                        
                        return Task.FromResult<IEnumerable<EntryPreviousItems>>(dbContext.EntryPreviousItems
                            .AsNoTracking()
                            .Where(exp)
                            .OrderBy(y => y.EntryPreviousItemId)
                            .Skip(startIndex)
                            .Take(count)
                            .ToList());
                    }
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<int> CountNav(string exp, Dictionary<string, string> navExp)
        {
            try
            {
                if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (exp == "All" && navExp.Count == 0)
                    {
                        return dbContext.EntryPreviousItems
										.AsNoTracking()
                                        .Count();
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "xcuda_Item":
                                return await CountWhere<PreviousEntry>(dbContext, exp, itm.Value, "EntryPreviousItems", "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
						}
                    }
                    return dbContext.EntryPreviousItems.Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
											.AsNoTracking()
                                            .Count();
                }
                
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<int> CountWhere<T>(PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, string rel) where T : class
        {
              switch (rel)
		    {
		        case "SelectMany":
                    return await CountWhereSelectMany<T>(dbContext, exp, navExp, navProp)
									.ConfigureAwait(continueOnCapturedContext: false);
		            
                default:
                    return await CountWhereSelect<T>(dbContext, exp, navExp, navProp)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static Task<int> CountWhereSelectMany<T>(PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp) where T : class
        {
			try
			{
            return Task.FromResult(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EntryPreviousItems>()
                .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
                .Distinct()
                .OrderBy("EntryPreviousItemId")
                .Count());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<int> CountWhereSelect<T>(PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp) where T : class
        {
			try
			{
            return Task.FromResult(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EntryPreviousItems>()
                .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
                .Distinct()
                .OrderBy("EntryPreviousItemId")
                .Count());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		  public async Task<IEnumerable<EntryPreviousItems>> LoadRangeNav(int startIndex, int count, string exp,
                                                                                 Dictionary<string, string> navExp, IEnumerable<string> includeLst = null)
        {
            try
            {
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if ((string.IsNullOrEmpty(exp) && navExp.Count == 0) || exp == "None") return new List<EntryPreviousItems>();
                    var set = AddIncludes(includeLst, dbContext);

                    if (exp == "All" && navExp.Count == 0)
                    {
                       
                        return set
									.AsNoTracking()
                                    .OrderBy(y => y.EntryPreviousItemId)
 
                                    .Skip(startIndex)
                                    .Take(count)
									.ToList();
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "xcuda_Item":
                                return
                                    await
                                        LoadRangeWhere<PreviousEntry>(startIndex, count, dbContext, exp, itm.Value, "EntryPreviousItems", "SelectMany")
													.ConfigureAwait(continueOnCapturedContext: false);

                          
							default:
                                throw new ArgumentException("No Navigation property found for " + itm.Key);
						}

                    }
                    return set//dbContext.EntryPreviousItems
								.AsNoTracking()
                                .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
								.OrderBy(y => y.EntryPreviousItemId)
 
                                .Skip(startIndex)
                                .Take(count)
								.ToList();


                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<IEnumerable<EntryPreviousItems>> LoadRangeWhere<T>(int startIndex, int count,
            PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, string rel, IEnumerable<string> includeLst = null) where T : class
        {
             switch (rel)
		    {
		        case "SelectMany":
                    return await LoadRangeSelectMany<T>(startIndex, count, dbContext, exp, navExp, navProp, includeLst)
									.ConfigureAwait(continueOnCapturedContext: false);
		            
                default:
                    return await LoadRangeSelect<T>(startIndex, count, dbContext, exp, navExp, navProp, includeLst)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static Task<IEnumerable<EntryPreviousItems>> LoadRangeSelectMany<T>(int startIndex, int count,
                                                                                    PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, IEnumerable<string> includeLst = null) where T : class
        {
			try
			{
            var set = dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EntryPreviousItems>();
    
            if (includeLst != null) set = includeLst.Aggregate(set, (current, itm) => current.Include(itm));            

            return Task.FromResult<IEnumerable<EntryPreviousItems>>(set
                .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
                .Distinct()
                .OrderBy(y => y.EntryPreviousItemId)
 
                .Skip(startIndex)
                .Take(count)
                .ToList());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<IEnumerable<EntryPreviousItems>> LoadRangeSelect<T>(int startIndex, int count,
                                                                                PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, IEnumerable<string> includeLst = null) where T : class
        {
			try
			{
              var set = dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EntryPreviousItems>();

               if (includeLst != null) set = includeLst.Aggregate(set, (current, itm) => current.Include(itm)); 
                
               return Task.FromResult<IEnumerable<EntryPreviousItems>>(set
                   .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
                   .Distinct()
                   .OrderBy(y => y.EntryPreviousItemId)
 
                   .Skip(startIndex)
                   .Take(count)
                   .ToList());
							 }
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

        private static async Task<IEnumerable<EntryPreviousItems>> GetWhere<T>(PreviousDocumentDSContext dbContext,
            string exp, string navExp, string navProp, string rel, List<string> includesLst = null) where T : class
        {
			try
			{
			    switch (rel)
				{
					case "SelectMany":
						return await GetWhereSelectMany<T>(dbContext, exp, navExp, navProp, includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);
						
					default:
						return await GetWhereSelect<T>(dbContext, exp, navExp, navProp, includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);
				}
			
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<IEnumerable<EntryPreviousItems>> GetWhereSelectMany<T>(PreviousDocumentDSContext dbContext,
                                                                                   string exp, string navExp, string navProp, List<string> includesLst = null) where T : class
        {
			try
			{

			if (includesLst == null)
			{
				return Task.FromResult<IEnumerable<EntryPreviousItems>>(dbContext.Set<T>()
                    .AsNoTracking()
                    .Where(navExp)
                    .SelectMany(navProp).OfType<EntryPreviousItems>()
                    .Where(exp == "All" || exp == null?"EntryPreviousItemId != null":exp)
                    .Distinct()
                    .ToList());
			}

			var set = (DbQuery<EntryPreviousItems>)dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EntryPreviousItems>()
                .Where(exp == "All" || exp == null?"EntryPreviousItemId != null":exp)
                .Distinct();

			set = includesLst.Aggregate(set, (current, itm) => current.Include(itm));

            return Task.FromResult<IEnumerable<EntryPreviousItems>>(set.ToList());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<IEnumerable<EntryPreviousItems>> GetWhereSelect<T>(PreviousDocumentDSContext dbContext,
                                                                               string exp, string navExp, string navProp, List<string> includesLst = null) where T : class
        {
			try
			{

			if (includesLst == null)
			{
				return Task.FromResult<IEnumerable<EntryPreviousItems>>(dbContext.Set<T>()
                    .AsNoTracking()
                    .Where(navExp)
                    .Select(navProp).OfType<EntryPreviousItems>()
                    .Where(exp == "All" || exp == null?"EntryPreviousItemId != null":exp)
                    .Distinct()
                    .ToList());
			}

			var set = (DbQuery<EntryPreviousItems>)dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EntryPreviousItems>()
                .Where(exp == "All" || exp == null?"EntryPreviousItemId != null":exp)
                .Distinct();

			set = includesLst.Aggregate(set, (current, itm) => current.Include(itm));

            return Task.FromResult<IEnumerable<EntryPreviousItems>>(set.ToList());
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

			        public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByPreviousItem_Id(string PreviousItem_Id, List<string> includesLst = null)
        {
            try
            {
                using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(PreviousItem_Id);
                var set = AddIncludes(includesLst, dbContext);
                IEnumerable<EntryPreviousItems> entities = set//dbContext.EntryPreviousItems
                                      .AsNoTracking()
                                        .Where(x => x.PreviousItem_Id.ToString() == PreviousItem_Id.ToString())
										.ToList();
                return Task.FromResult(entities);
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
 	        public Task<IEnumerable<EntryPreviousItems>> GetEntryPreviousItemsByItem_Id(string Item_Id, List<string> includesLst = null)
        {
            try
            {
                using ( var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(Item_Id);
                var set = AddIncludes(includesLst, dbContext);
                IEnumerable<EntryPreviousItems> entities = set//dbContext.EntryPreviousItems
                                      .AsNoTracking()
                                        .Where(x => x.Item_Id.ToString() == Item_Id.ToString())
										.ToList();
                return Task.FromResult(entities);
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
 
		public decimal SumField(string whereExp, string field)
         {
             try
             {
                 using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                 {
                    dbContext.Database.CommandTimeout = 0;
					decimal res = 0;
                     if (string.IsNullOrEmpty(whereExp) || whereExp == "None") return 0;
                     if (whereExp == "All")
                     {
                          res = Convert.ToDecimal(dbContext.EntryPreviousItems.AsNoTracking().Sum(field));
                     }
                     else
                     {
                         res = Convert.ToDecimal(dbContext.EntryPreviousItems.AsNoTracking().Where(whereExp).Sum(field));
                     }
                     
                     return res;
                 }
             }
             catch (Exception updateEx)
             {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
             }
         }

        public async Task<decimal> SumNav( string exp, Dictionary<string, string> navExp, string field)
        {
            try
            {
                if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (!dbContext.EntryPreviousItems.Any()) return 0;
                    if (exp == "All" && navExp.Count == 0)
                    {
                        return Convert.ToDecimal(dbContext.EntryPreviousItems
										.AsNoTracking()
                                        .Sum(field)??0);
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "xcuda_Item":
                                return await SumWhere<PreviousEntry>(dbContext, exp, itm.Value, "EntryPreviousItems", field, "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
						}
                    }
                    return Convert.ToDecimal(dbContext.EntryPreviousItems.Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
											.AsNoTracking()
                                            .Sum(field)??0);
                }
                
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<decimal> SumWhere<T>(PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, string field, string rel) where T : class
        {
              switch (rel)
		    {
		        case "SelectMany":
                    return await SumWhereSelectMany<T>(dbContext, exp, navExp, navProp, field)
									.ConfigureAwait(continueOnCapturedContext: false);
		        default:
                    return await SumWhereSelect<T>(dbContext, exp, navExp, navProp, field)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static Task<decimal> SumWhereSelectMany<T>(PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, string field) where T : class
        {
			try
			{
            return Task.FromResult(Convert.ToDecimal(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<EntryPreviousItems>()
                .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
                .Distinct()
                .OrderBy("EntryPreviousItemId")
                .Sum(field)));
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static Task<decimal> SumWhereSelect<T>(PreviousDocumentDSContext dbContext, string exp, string navExp, string navProp, string field) where T : class
        {
			try
			{
            return Task.FromResult(Convert.ToDecimal(dbContext.Set<T>()
                .AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<EntryPreviousItems>()
                .Where(exp == "All" || exp == null ? "EntryPreviousItemId != null" : exp)
                .Distinct()
                .OrderBy("EntryPreviousItemId")
                .Sum(field)));
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }



		 public string MinField(string whereExp, string field)
         {
             try
             {
                 using (var dbContext = new PreviousDocumentDSContext(){StartTracking = StartTracking})
                 {
                    dbContext.Database.CommandTimeout = 0;
					string res = "";
                     if (string.IsNullOrEmpty(whereExp) || whereExp == "None") return res;
                     if (whereExp == "All")
                     {
                          res = Convert.ToString(dbContext.EntryPreviousItems.AsNoTracking().Min(field));
                     }
                     else
                     {
                         res = Convert.ToString(dbContext.EntryPreviousItems.AsNoTracking().Where(whereExp).Min(field));
                     }
                     
                     return res;
                 }
             }
             catch (Exception updateEx)
             {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
             }
         }

		 
		private static IQueryable<EntryPreviousItems> AddIncludes(IEnumerable<string> includesLst, PreviousDocumentDSContext dbContext)
       {
		 try
			{
			   if (includesLst == null) includesLst = new List<string>();
			   var set =(DbQuery<EntryPreviousItems>) dbContext.EntryPreviousItems; 
			   set = includesLst.Where(x => !string.IsNullOrEmpty(x))
                                .Aggregate(set, (current, itm) => current.Include(itm));
			   return set;
			 }
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
       }
	   private IQueryable<EntryPreviousItems> AddWheres(List<string> expLst, IQueryable<EntryPreviousItems> set)
        {
            try
            {
                return expLst.Where(x => !string.IsNullOrEmpty(x))
                             .Aggregate(set, (current, itm) => current.Where(itm));
            }
            catch (Exception)
            {
                Debugger.Break();
                throw;
            }
          
        }

        public void Dispose()
        {
            try
            {
               // var dispose = dbContext as IDisposable;
             ////   if (dispose != null)
            //    {
           //         dbContext.Dispose();
           //     }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }

        }

    }
}



