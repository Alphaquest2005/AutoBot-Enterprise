// <autogenerated>
//   This file was generated by T4 code generator AllServices.tt.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>


using System;
using System.Collections.Generic;
using System.Collections.Concurrent;
using System.Data.Entity.Infrastructure;
using System.Data.Entity.Core;
using System.Data.SqlClient;
using System.Diagnostics;
using System.ServiceModel;
using System.Threading.Tasks;
//using System.Transactions;


using System.Linq.Dynamic;
using System.ComponentModel.Composition;
using InventoryDS.Business.Entities;
using Core.Common.Contracts;
using Core.Common.Business.Services;
using Core.Common.UI;

using System.Data.Entity;
using System.Linq;
using TrackableEntities;
using TrackableEntities.Common;
using TrackableEntities.EF6;
using WaterNut.Interfaces;

namespace InventoryDS.Business.Services
{
   [Export (typeof(IInventoryItemAliaService))]
   [Export(typeof(IBusinessService))]
   [PartCreationPolicy(CreationPolicy.NonShared)]
   [ServiceBehavior(InstanceContextMode = InstanceContextMode.PerCall,
                    ConcurrencyMode = ConcurrencyMode.Multiple)]
   
    public partial class InventoryItemAliaService : IInventoryItemAliaService, IDisposable
    {
        //private readonly InventoryDSContext dbContext;

        public bool StartTracking { get; set; }

        public InventoryItemAliaService()
        {
            try
            {
                // dbContext = new InventoryDSContext(){StartTracking = StartTracking};
                StartTracking = false;
             }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault, new FaultReason(fault.Message));
            }
        }

        public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAlias(List<string> includesLst = null, bool tracking = true)
        {
            try
            {
            //using (var scope = new TransactionScope(TransactionScopeOption.Required,
                                   //new TransactionOptions() {IsolationLevel = IsolationLevel.ReadUncommitted}))
               // {
                  using ( var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                  {
				    var set = AddIncludes(includesLst, dbContext);
                    IEnumerable<InventoryItemAlia> entities = await set.AsNoTracking().ToListAsync()
													       .ConfigureAwait(continueOnCapturedContext: false);
                           //scope.Complete();
                            if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                            return entities;
                   }
                //}
             }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }


        public async Task<InventoryItemAlia> GetInventoryItemAliaByKey(string AliasId, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
			   if(string.IsNullOrEmpty(AliasId))return null; 
              using ( var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(AliasId);
				var set = AddIncludes(includesLst, dbContext);
                InventoryItemAlia entity = await set.AsNoTracking().SingleOrDefaultAsync(x => x.AliasId == i).ConfigureAwait(continueOnCapturedContext: false);
                if(tracking && entity != null) entity.StartTracking();
                return entity;
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }


		 public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAliasByExpression(string exp, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
					if (string.IsNullOrEmpty(exp) || exp == "None") return new List<InventoryItemAlia>();
					var set = AddIncludes(includesLst, dbContext);
                    if (exp == "All")
                    {
						var entities = await set.AsNoTracking().ToListAsync()
											.ConfigureAwait(continueOnCapturedContext: false);

                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 
                    }
					else
					{
						var entities = await set.AsNoTracking().Where(exp)
											.ToListAsync() 
											.ConfigureAwait(continueOnCapturedContext: false);
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 
											
					}
					
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		 public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAliasByExpressionLst(List<string> expLst, List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
					if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return new List<InventoryItemAlia>();
					var set = AddIncludes(includesLst, dbContext);
                    if (expLst.FirstOrDefault() == "All")
                    {
						var entities = await set.AsNoTracking().ToListAsync()
											.ConfigureAwait(continueOnCapturedContext: false); 
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 
                    }
					else
					{
						set = AddWheres(expLst, set);
						var entities = await set.AsNoTracking().ToListAsync() 
										.ConfigureAwait(continueOnCapturedContext: false);
                        if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 
											
					}
					
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAliasByExpressionNav(string exp,
																							  Dictionary<string, string> navExp,
																							  List<string> includesLst = null, bool tracking = true)
        {
            try
            {
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (string.IsNullOrEmpty(exp) || exp == "None") return new List<InventoryItemAlia>();

                    if (exp == "All" && navExp.Count == 0)
                    {
                        var aentities = await AddIncludes(includesLst, dbContext)
												.ToListAsync()
												.ConfigureAwait(continueOnCapturedContext: false);
                        if(tracking) aentities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return aentities; 
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "InventoryItem":
                                return
                                    await
                                        GetWhere<InventoryItem>(dbContext, exp, itm.Value, "InventoryItemAlias", "SelectMany", includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);

                        }

                    }
					var set = AddIncludes(includesLst, dbContext);
                    var entities = await set.AsNoTracking().Where(exp)
									.ToListAsync()
									.ConfigureAwait(continueOnCapturedContext: false);
                    if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                        return entities; 

                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAliasByBatch(string exp,
            int totalrow, List<string> includesLst = null, bool tracking = true)
        {
            try
            {

                var res = new ConcurrentQueue<List<InventoryItemAlia>>();



                if (string.IsNullOrEmpty(exp) || exp == "None") return new List<InventoryItemAlia>();


                var batchSize = 500;
                var batches = Convert.ToInt32(totalrow / batchSize);

                if (totalrow % batchSize > 0) batches += 1;
                var exceptions = new ConcurrentQueue<Exception>();
                Parallel.For(0, batches,
                   new ParallelOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount * 2 },
                    bat =>
                    //  for (int bat = 0; bat < batches; bat++)
                    {
                        try
                        {
                            using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                            {
                                dbContext.Database.CommandTimeout = 0;
                                dbContext.Configuration.AutoDetectChangesEnabled = false;
                                //dbContext.Configuration.LazyLoadingEnabled = true;
                                var set = AddIncludes(includesLst, dbContext);
                                IQueryable<InventoryItemAlia> dset;
                                if (exp == "All")
                                {
                                    dset = set.OrderBy(x => x.AliasId);
                                }
                                else
                                {
                                    dset = set.OrderBy(x => x.AliasId).Where(exp);
                                }

                                var lst = dset.AsNoTracking()
                                    .Skip(bat * batchSize)
                                    .Take(batchSize)
                                    .ToList();
                                res.Enqueue(lst);
                            }

                        }
                        catch (Exception ex)
                        {
                            exceptions.Enqueue(ex);
                        }
                    }
                    );
                if (exceptions.Count > 0) throw new AggregateException(exceptions);
    
                var entities = res.SelectMany(x => x.ToList());
                if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                return entities; 

            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }
        public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAliasByBatchExpressionLst(List<string> expLst,
            int totalrow, List<string> includesLst = null, bool tracking = true)
        {
            try
            {

                var res = new ConcurrentQueue<List<InventoryItemAlia>>();



                if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return new List<InventoryItemAlia>();


                var batchSize = 500;
                var batches = Convert.ToInt32(totalrow / batchSize);

                if (totalrow % batchSize > 0) batches += 1;
                var exceptions = new ConcurrentQueue<Exception>();
                Parallel.For(0, batches,
                   new ParallelOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount * 2 },
                    bat =>
                    //  for (int bat = 0; bat < batches; bat++)
                    {
                        try
                        {
                            using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                            {
                                dbContext.Database.CommandTimeout = 0;
                                dbContext.Configuration.AutoDetectChangesEnabled = false;
                                //dbContext.Configuration.LazyLoadingEnabled = true;
                                var set = AddIncludes(includesLst, dbContext);
                                IQueryable<InventoryItemAlia> dset;
                                if (expLst.FirstOrDefault() == "All")
                                {
                                    dset = set.OrderBy(x => x.AliasId);
                                }
                                else
                                {
                                    set = AddWheres(expLst, set);
                                    dset = set.OrderBy(x => x.AliasId);
                                }

                                var lst = dset.AsNoTracking()
                                    .Skip(bat * batchSize)
                                    .Take(batchSize)
                                    .ToList();
                                res.Enqueue(lst);
                            }

                        }
                        catch (Exception ex)
                        {
                            exceptions.Enqueue(ex);
                        }
                    }
                    );
                if (exceptions.Count > 0) throw new AggregateException(exceptions);
                var entities = res.SelectMany(x => x.ToList());
                if(tracking) entities.AsParallel(new ParallelLinqOptions() { MaxDegreeOfParallelism = Environment.ProcessorCount }).ForAll(x => x.StartTracking());
                return entities; 
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }


        public async Task<InventoryItemAlia> UpdateInventoryItemAlia(InventoryItemAlia entity)
        { 
            using ( var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
              {
                try
                {   
                     var res = (InventoryItemAlia) entity;
                    if(res.TrackingState == TrackingState.Unchanged) res.TrackingState = TrackingState.Modified;                              
                    
                    dbContext.ApplyChanges(res);
                    await dbContext.SaveChangesAsync().ConfigureAwait(continueOnCapturedContext: false);
                    res.AcceptChanges();
                    return res;      

                   // var entitychanges = entity.ChangeTracker.GetChanges();
                   // if (entitychanges != null && entitychanges.FirstOrDefault() != null)
                   // {
                   //     dbContext.ApplyChanges(entitychanges);
                   //     await dbContext.SaveChangesAsync().ConfigureAwait(continueOnCapturedContext: false);
                   //     entity.EntityId = entitychanges.FirstOrDefault().EntityId;
                   //     entity.AcceptChanges();     
                   // }
                   // return entity;        
                }
                catch (DbUpdateConcurrencyException dce)
                {
                    // Get failed entry
                    foreach (var itm in dce.Entries)
                    {
                        if(itm.State != EntityState.Added)
                         {
                            var dv = itm.GetDatabaseValues();
                            if(dv != null) itm.OriginalValues.SetValues(dv);
                        }
                    }
                }
                catch (OptimisticConcurrencyException oce)
                {
                    var context = ((IObjectContextAdapter)dbContext).ObjectContext;

                    foreach (var entry in oce.StateEntries)
                    {
                        context.Refresh(System.Data.Entity.Core.Objects.RefreshMode.StoreWins, entry.Entity);
                    }
                }   
                catch (DbUpdateException e)
                {
                    
                   // Debugger.Break();
                    SqlException s = e.InnerException.InnerException as SqlException;
                    if (s != null && s.Number == 2627)
                    {
                         
                    }
                    else
                    {
                        Debugger.Break();
                        throw;
                    }
                }             
                catch (Exception updateEx)
                {
                    if (
                        updateEx.Message.Contains(
                            "The changes to the database were committed successfully, " +
                            "but an error occurred while updating the object context"))
                        return entity;

                    System.Diagnostics.Debugger.Break();
                    //throw new FaultException(updateEx.Message);
                        var fault = new ValidationFault
                                    {
                                        Result = false,
                                        Message = updateEx.Message,
                                        Description = updateEx.StackTrace
                                    };
                        throw new FaultException<ValidationFault>(fault);
                }
            }
           return entity;
        }

        public async Task<InventoryItemAlia> CreateInventoryItemAlia(InventoryItemAlia entity)
        {
            try
            {
                var res = (InventoryItemAlia) entity;
              using ( var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
              {
                dbContext.InventoryItemAlias.Add(res);
                await dbContext.SaveChangesAsync().ConfigureAwait(continueOnCapturedContext: false);
                res.AcceptChanges();
                return res;
              }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public async Task<bool> DeleteInventoryItemAlia(string AliasId)
        {
            try
            {
              using ( var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(AliasId);
                InventoryItemAlia entity = await dbContext.InventoryItemAlias
													.SingleOrDefaultAsync(x => x.AliasId == i)
													.ConfigureAwait(continueOnCapturedContext: false);
                if (entity == null)
                    return false;

                    dbContext.InventoryItemAlias.Attach(entity);
                    dbContext.InventoryItemAlias.Remove(entity);
                    await dbContext.SaveChangesAsync().ConfigureAwait(continueOnCapturedContext: false);
                    return true;
              }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

        public async Task<bool> RemoveSelectedInventoryItemAlia(IEnumerable<string> lst)
        {
            try
            {
                StatusModel.StartStatusUpdate("Removing InventoryItemAlia", lst.Count());
                var t = Task.Run(() =>
                {
                    using (var ctx = new InventoryItemAliaService())
                    {
                        foreach (var item in lst.ToList())
                        {

                            ctx.DeleteInventoryItemAlia(item).Wait();
                            StatusModel.StatusUpdate();
                        }
                    }
                });
                await t.ConfigureAwait(false);
                return true;
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
            

               
        }

		// Virtural list Implementation

         public async Task<int> CountByExpressionLst(List<string> expLst)
        {
            try
            {
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (expLst.Count == 0 || expLst.FirstOrDefault() == "None") return 0;
                    var set = (IQueryable<InventoryItemAlia>)dbContext.InventoryItemAlias; 
                    if (expLst.FirstOrDefault() == "All")
                    {
                        return await set.AsNoTracking().CountAsync()
                                            .ConfigureAwait(continueOnCapturedContext: false);
                    }
                    else
                    {
                        set = AddWheres(expLst, set);
                        return await set.AsNoTracking().CountAsync()
                                        .ConfigureAwait(continueOnCapturedContext: false);
                    }
                    
                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<int> Count(string exp)
        {
            try
            {
                using (InventoryDSContext dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                    if (exp == "All")
                    {
                        return await dbContext.InventoryItemAlias
                                    .AsNoTracking()
									.CountAsync()
									.ConfigureAwait(continueOnCapturedContext: false);
                    }
                    else
                    {
                        
                        return await dbContext.InventoryItemAlias
									.AsNoTracking()
                                    .Where(exp)
									.CountAsync()
									.ConfigureAwait(continueOnCapturedContext: false);
                    }
                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
        
        public async Task<IEnumerable<InventoryItemAlia>> LoadRange(int startIndex, int count, string exp)
        {
            try
            {
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (string.IsNullOrEmpty(exp) || exp == "None") return new List<InventoryItemAlia>();
                    if (exp == "All")
                    {
                        return await dbContext.InventoryItemAlias
										.AsNoTracking()
                                        .OrderBy(y => y.AliasId)
										.Skip(startIndex)
										.Take(count)
										.ToListAsync()
										.ConfigureAwait(continueOnCapturedContext: false);
                    }
                    else
                    {
                        
                        return await dbContext.InventoryItemAlias
										.AsNoTracking()
                                        .Where(exp)
										.OrderBy(y => y.AliasId)
										.Skip(startIndex)
										.Take(count)
										.ToListAsync()
										.ConfigureAwait(continueOnCapturedContext: false);
                    }
                }
            }
            catch (Exception updateEx)
            {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		public async Task<int> CountNav(string exp, Dictionary<string, string> navExp)
        {
            try
            {
                if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (exp == "All" && navExp.Count == 0)
                    {
                        return await dbContext.InventoryItemAlias
										.AsNoTracking()
                                        .CountAsync()
										.ConfigureAwait(continueOnCapturedContext: false);
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "InventoryItem":
                                return await CountWhere<InventoryItem>(dbContext, exp, itm.Value, "InventoryItemAlias", "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
						}
                    }
                    return await dbContext.InventoryItemAlias.Where(exp == "All" || exp == null ? "AliasId != null" : exp)
											.AsNoTracking()
                                            .CountAsync()
											.ConfigureAwait(continueOnCapturedContext: false);
                }
                
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<int> CountWhere<T>(InventoryDSContext dbContext, string exp, string navExp, string navProp, string rel) where T : class
        {
              switch (rel)
		    {
		        case "SelectMany":
                    return await CountWhereSelectMany<T>(dbContext, exp, navExp, navProp)
									.ConfigureAwait(continueOnCapturedContext: false);
		            
                default:
                    return await CountWhereSelect<T>(dbContext, exp, navExp, navProp)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static async Task<int> CountWhereSelectMany<T>(InventoryDSContext dbContext, string exp, string navExp, string navProp) where T : class
        {
			try
			{
            return await dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<InventoryItemAlia>()
                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
                .Distinct()
                .OrderBy("AliasId")
                .CountAsync()
				.ConfigureAwait(continueOnCapturedContext: false);
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static async Task<int> CountWhereSelect<T>(InventoryDSContext dbContext, string exp, string navExp, string navProp) where T : class
        {
			try
			{
            return await dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<InventoryItemAlia>()
                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
                .Distinct()
                .OrderBy("AliasId")
                .CountAsync()
				.ConfigureAwait(continueOnCapturedContext: false);
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		  public async Task<IEnumerable<InventoryItemAlia>> LoadRangeNav(int startIndex, int count, string exp,
                                                                                 Dictionary<string, string> navExp, IEnumerable<string> includeLst = null)
        {
            try
            {
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if ((string.IsNullOrEmpty(exp) && navExp.Count == 0) || exp == "None") return new List<InventoryItemAlia>();
                    var set = AddIncludes(includeLst, dbContext);

                    if (exp == "All" && navExp.Count == 0)
                    {
                       
                        return await set
									.AsNoTracking()
                                    .OrderBy(y => y.AliasId)
 
                                    .Skip(startIndex)
                                    .Take(count)
									.ToListAsync()
									.ConfigureAwait(continueOnCapturedContext: false);
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "InventoryItem":
                                return
                                    await
                                        LoadRangeWhere<InventoryItem>(startIndex, count, dbContext, exp, itm.Value, "InventoryItemAlias", "SelectMany")
													.ConfigureAwait(continueOnCapturedContext: false);

                          
							default:
                                throw new ArgumentException("No Navigation property found for " + itm.Key);
						}

                    }
                    return await set//dbContext.InventoryItemAlias
								.AsNoTracking()
                                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
								.OrderBy(y => y.AliasId)
 
                                .Skip(startIndex)
                                .Take(count)
								.ToListAsync()
								.ConfigureAwait(continueOnCapturedContext: false);


                }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<IEnumerable<InventoryItemAlia>> LoadRangeWhere<T>(int startIndex, int count,
            InventoryDSContext dbContext, string exp, string navExp, string navProp, string rel, IEnumerable<string> includeLst = null) where T : class
        {
             switch (rel)
		    {
		        case "SelectMany":
                    return await LoadRangeSelectMany<T>(startIndex, count, dbContext, exp, navExp, navProp, includeLst)
									.ConfigureAwait(continueOnCapturedContext: false);
		            
                default:
                    return await LoadRangeSelect<T>(startIndex, count, dbContext, exp, navExp, navProp, includeLst)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static async Task<IEnumerable<InventoryItemAlia>> LoadRangeSelectMany<T>(int startIndex, int count,
            InventoryDSContext dbContext, string exp, string navExp, string navProp, IEnumerable<string> includeLst = null) where T : class
        {
			try
			{
            var set = dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<InventoryItemAlia>();
    
            if (includeLst != null) set = includeLst.Aggregate(set, (current, itm) => current.Include(itm));            

            return await set
                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
                .Distinct()
                .OrderBy(y => y.AliasId)
 
                .Skip(startIndex)
                .Take(count)
                .ToListAsync()
				.ConfigureAwait(continueOnCapturedContext: false);
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static async Task<IEnumerable<InventoryItemAlia>> LoadRangeSelect<T>(int startIndex, int count,
            InventoryDSContext dbContext, string exp, string navExp, string navProp, IEnumerable<string> includeLst = null) where T : class
        {
			try
			{
              var set = dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<InventoryItemAlia>();

               if (includeLst != null) set = includeLst.Aggregate(set, (current, itm) => current.Include(itm)); 
                
               return await set
                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
                .Distinct()
                .OrderBy(y => y.AliasId)
 
                .Skip(startIndex)
                .Take(count)
                .ToListAsync()
				.ConfigureAwait(continueOnCapturedContext: false);
							 }
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

        private static async Task<IEnumerable<InventoryItemAlia>> GetWhere<T>(InventoryDSContext dbContext,
            string exp, string navExp, string navProp, string rel, List<string> includesLst = null) where T : class
        {
			try
			{
			    switch (rel)
				{
					case "SelectMany":
						return await GetWhereSelectMany<T>(dbContext, exp, navExp, navProp, includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);
						
					default:
						return await GetWhereSelect<T>(dbContext, exp, navExp, navProp, includesLst)
										.ConfigureAwait(continueOnCapturedContext: false);
				}
			
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static async Task<IEnumerable<InventoryItemAlia>> GetWhereSelectMany<T>(InventoryDSContext dbContext,
            string exp, string navExp, string navProp, List<string> includesLst = null) where T : class
        {
			try
			{

			if (includesLst == null)
			{
				return await dbContext.Set<T>()
							.AsNoTracking()
                            .Where(navExp)
							.SelectMany(navProp).OfType<InventoryItemAlia>()
							.Where(exp == "All" || exp == null?"AliasId != null":exp)
							.Distinct()
							.ToListAsync()
							.ConfigureAwait(continueOnCapturedContext: false);
			}

			var set = (DbQuery<InventoryItemAlia>)dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<InventoryItemAlia>()
                .Where(exp == "All" || exp == null?"AliasId != null":exp)
                .Distinct();

			set = includesLst.Aggregate(set, (current, itm) => current.Include(itm));

            return await set.ToListAsync()
							.ConfigureAwait(continueOnCapturedContext: false);
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static async Task<IEnumerable<InventoryItemAlia>> GetWhereSelect<T>(InventoryDSContext dbContext,
            string exp, string navExp, string navProp, List<string> includesLst = null) where T : class
        {
			try
			{

			if (includesLst == null)
			{
				return await dbContext.Set<T>()
							.AsNoTracking()
                            .Where(navExp)
							.Select(navProp).OfType<InventoryItemAlia>()
							.Where(exp == "All" || exp == null?"AliasId != null":exp)
							.Distinct()
							.ToListAsync()
							.ConfigureAwait(continueOnCapturedContext: false);
			}

			var set = (DbQuery<InventoryItemAlia>)dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<InventoryItemAlia>()
                .Where(exp == "All" || exp == null?"AliasId != null":exp)
                .Distinct();

			set = includesLst.Aggregate(set, (current, itm) => current.Include(itm));

            return await set.ToListAsync()
							.ConfigureAwait(continueOnCapturedContext: false);
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

			        public async Task<IEnumerable<InventoryItemAlia>> GetInventoryItemAliaByInventoryItemId(string InventoryItemId, List<string> includesLst = null)
        {
            try
            {
                using ( var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
              {
                var i = Convert.ToInt32(InventoryItemId);
                var set = AddIncludes(includesLst, dbContext);
                IEnumerable<InventoryItemAlia> entities = await set//dbContext.InventoryItemAlias
                                      .AsNoTracking()
                                        .Where(x => x.InventoryItemId.ToString() == InventoryItemId.ToString())
										.ToListAsync()
										.ConfigureAwait(continueOnCapturedContext: false);
                return entities;
              }
             }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }
        }
 
		public decimal SumField(string whereExp, string field)
         {
             try
             {
                 using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                 {
                    dbContext.Database.CommandTimeout = 0;
					decimal res = 0;
                     if (string.IsNullOrEmpty(whereExp) || whereExp == "None") return 0;
                     if (whereExp == "All")
                     {
                          res = Convert.ToDecimal(dbContext.InventoryItemAlias.AsNoTracking().Sum(field));
                     }
                     else
                     {
                         res = Convert.ToDecimal(dbContext.InventoryItemAlias.AsNoTracking().Where(whereExp).Sum(field));
                     }
                     
                     return res;
                 }
             }
             catch (Exception updateEx)
             {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
             }
         }

        public async Task<decimal> SumNav( string exp, Dictionary<string, string> navExp, string field)
        {
            try
            {
                if (string.IsNullOrEmpty(exp) || exp == "None") return 0;
                using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                {
                    dbContext.Database.CommandTimeout = 0;
                    if (!dbContext.InventoryItemAlias.Any()) return 0;
                    if (exp == "All" && navExp.Count == 0)
                    {
                        return Convert.ToDecimal(dbContext.InventoryItemAlias
										.AsNoTracking()
                                        .Sum(field)??0);
                    }
                    foreach (var itm in navExp)
                    {
                        switch (itm.Key)
                        {
                            case "InventoryItem":
                                return await SumWhere<InventoryItem>(dbContext, exp, itm.Value, "InventoryItemAlias", field, "SelectMany")
											.ConfigureAwait(continueOnCapturedContext: false);
						}
                    }
                    return Convert.ToDecimal(dbContext.InventoryItemAlias.Where(exp == "All" || exp == null ? "AliasId != null" : exp)
											.AsNoTracking()
                                            .Sum(field)??0);
                }
                
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                var fault = new ValidationFault
                {
                    Result = false,
                    Message = updateEx.Message,
                    Description = updateEx.StackTrace
                };
                throw new FaultException<ValidationFault>(fault);
            }
        }

		private static async Task<decimal> SumWhere<T>(InventoryDSContext dbContext, string exp, string navExp, string navProp, string field, string rel) where T : class
        {
              switch (rel)
		    {
		        case "SelectMany":
                    return await SumWhereSelectMany<T>(dbContext, exp, navExp, navProp, field)
									.ConfigureAwait(continueOnCapturedContext: false);
		        default:
                    return await SumWhereSelect<T>(dbContext, exp, navExp, navProp, field)
									.ConfigureAwait(continueOnCapturedContext: false);
		    }
        }

		private static async Task<decimal> SumWhereSelectMany<T>(InventoryDSContext dbContext, string exp, string navExp, string navProp, string field) where T : class
        {
			try
			{
            return Convert.ToDecimal(dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .SelectMany(navProp).OfType<InventoryItemAlia>()
                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
                .Distinct()
                .OrderBy("AliasId")
                .Sum(field));
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }

		private static async Task<decimal> SumWhereSelect<T>(InventoryDSContext dbContext, string exp, string navExp, string navProp, string field) where T : class
        {
			try
			{
            return Convert.ToDecimal(dbContext.Set<T>()
				.AsNoTracking()
                .Where(navExp)
                .Select(navProp).OfType<InventoryItemAlia>()
                .Where(exp == "All" || exp == null ? "AliasId != null" : exp)
                .Distinct()
                .OrderBy("AliasId")
                .Sum(field));
			}
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
        }



		 public string MinField(string whereExp, string field)
         {
             try
             {
                 using (var dbContext = new InventoryDSContext(){StartTracking = StartTracking})
                 {
                    dbContext.Database.CommandTimeout = 0;
					string res = "";
                     if (string.IsNullOrEmpty(whereExp) || whereExp == "None") return res;
                     if (whereExp == "All")
                     {
                          res = Convert.ToString(dbContext.InventoryItemAlias.AsNoTracking().Min(field));
                     }
                     else
                     {
                         res = Convert.ToString(dbContext.InventoryItemAlias.AsNoTracking().Where(whereExp).Min(field));
                     }
                     
                     return res;
                 }
             }
             catch (Exception updateEx)
             {
                    System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
             }
         }

		 
		private static IQueryable<InventoryItemAlia> AddIncludes(IEnumerable<string> includesLst, InventoryDSContext dbContext)
       {
		 try
			{
			   if (includesLst == null) includesLst = new List<string>();
			   var set =(DbQuery<InventoryItemAlia>) dbContext.InventoryItemAlias; 
			   set = includesLst.Where(x => !string.IsNullOrEmpty(x))
                                .Aggregate(set, (current, itm) => current.Include(itm));
			   return set;
			 }
			catch (Exception)
			{
				Debugger.Break();
				throw;
			}
       }
	   private IQueryable<InventoryItemAlia> AddWheres(List<string> expLst, IQueryable<InventoryItemAlia> set)
        {
            try
            {
                return expLst.Where(x => !string.IsNullOrEmpty(x))
                             .Aggregate(set, (current, itm) => current.Where(itm));
            }
            catch (Exception)
            {
                Debugger.Break();
                throw;
            }
          
        }

        public void Dispose()
        {
            try
            {
               // var dispose = dbContext as IDisposable;
             ////   if (dispose != null)
            //    {
           //         dbContext.Dispose();
           //     }
            }
            catch (Exception updateEx)
            {
                System.Diagnostics.Debugger.Break();
                //throw new FaultException(updateEx.Message);
                    var fault = new ValidationFault
                                {
                                    Result = false,
                                    Message = updateEx.Message,
                                    Description = updateEx.StackTrace
                                };
                    throw new FaultException<ValidationFault>(fault);
            }

        }

    }
}



